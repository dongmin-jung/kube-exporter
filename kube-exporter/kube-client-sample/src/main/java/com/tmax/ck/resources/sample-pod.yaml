apiVersion: v1
kind: Pod
metadata:
  name: sample-pod
  namespace: dongmin
spec:
  containers:
    - name: ubuntu
      image: ubuntu:latest
      command: ["/bin/sleep", "3650d"]
      resources:
        limits:
          cpu: '1'
          memory: 2Gi